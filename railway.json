{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate && python manage.py create_superuser && gunicorn timetable_backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
